#include "HamManager.h"

/*
const int HamConfig::MAX_PORT_NUM = 10;
const int HamConfig::MIN_PORT_NUM = 1;

HamConfig::HamConfig(const std::string port, const std::- freq, const std::string callSign)
{
	if (callSign.size() == 0) {
		genDefaultPort();
	}
}

void HamConfig::genDefaultPort()
{
	for (int i = MIN_PORT_NUM; i < MAX_PORT_NUM; i++) {
		std::string comPort;
#ifdef _WIN32
		
#else

#endif
	}
}
*/